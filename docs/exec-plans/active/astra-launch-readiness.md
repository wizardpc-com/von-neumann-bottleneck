# Astra competition readiness

2026-09-14. User authorizes competition-focused iteration; keep five regions / 40
tasks and all original construction, simulation, save and Hint rules.

## Scope and rationale

- Improve an international newcomer's entry into the existing game, not add an
  unrelated AI mechanic or a new chapter to qualify for a competition.
- Native-test the English tutorial and branch transition; fix observed UI blockers.
- Prepare bilingual quickstart, truthful submission fields and direct runtime
  gallery with source provenance. No public submission, deployment or credentials.
- Preserve current frozen packages; create a new identity only after runtime changes
  are committed. Platform acceptance remains separate from automatic checks.

## Files and verification

Hub / WindowMode language navigation, hardware palette minimum, existing settings
and hardware suites; docs/distribution/astra-challenge; README links. Run isolated
Godot 4.7.1 checks, native locale/restart/tutorial paths, inspect gallery and diff.
User data remains isolated. No level/catalog/model changes.

## Progress

- Official challenge and Product Hunt prep pages checked. Submission date/eligibility
  details behind login still need owner verification; public countdown is unreliable.
- Homepage language selection uses existing preferences and safe scene reload,
  without forcing Settings open afterward. Four affected suites pass at first revision.
- Native new English player connected both tutorial wires, changed input, ran the
  circuit: 3/5 actions reached. Found first palette card clipped after initial layout;
  correction raises the palette minimum to show its first card.
- Submission fields, bilingual first-session instructions, development-evidence
  boundaries and showcase storyboard prepared. Final verification pending.

## Remaining owner gates

Final Windows/macOS package interaction, outside-player comprehension, exact
contest eligibility/model attribution, chosen public download and submission.

## Owner steering: window and OS conventions

User explicitly asks for freely draggable windows and less platform cognitive
load. Existing floating instruments already support drag/resize. Extend the two
fixed reading/settings surfaces with bounded title dragging; add macOS ⌘, settings
and ⌃⌘F fullscreen, retain existing Windows shortcuts and text-first editing. Do
not turn confirmations into freely interacting workbenches or change OS-global
shortcuts. Verify cancellation, resize bounds and both locale layouts.
