# Chinese copy audit and integration

Scope: user-approved second audit of the supplied MiMo decisions, with additional
Chinese title/intro/transition editing. User's latest style direction supersedes
the attachment's instruction to accept its 82 decisions unchanged. Keep technical
specifications, Hint reveal levels, model, progression, saves and privacy intact.
No existing English copy changes; no public distribution.

Inputs: supplied audit ZIP, audit report and import prompt in Downloads. The source
MiMo input/output/validator archive is absent; do not claim its original validator
was run. Audit package includes all 82 baseline/output/approved records and hashes.
Independent integrity, placeholder/token/link checks and source-drift checks apply.
82 baseline hashes match. All 77 real PO entries match the declared source commit
18b37fc; 38 differ from the unimported editorial baseline, not from repository drift.

Steps: inspect all decisions; record final text/reason/current-source/category;
synchronize five chapter title surfaces and selected task names; separate Chapter 3
technical eyebrow without changing visible English; validate approved-only deltas,
protected syntax, localization and source behavior; render normal Game Chinese UI
and representative bilingual/minimum-size fixtures; commit/build candidate and record
real native versus automated evidence. Preserve user downloads and real player data.

Title direction: 一线成机 / 时间的去处 / 少走远路 / 与等待并行 / 各就其位.
Technical subtitles retain the concrete subject. Existing component names remain
technical names; lighter imagery is reserved for chapter/application task titles.

Audit findings: Chapter 1 now includes a software optimization optional task, so its
intro cannot claim the entire chapter excludes software optimization. Full-adder
summary must describe A+B plus incoming carry, not the result receiving a carry.
Retain the capstone first-experiment scope and parity's one-then-two-bit counterexample.

Rendered review also exposed an existing overlap between selector and parity in the legacy prologue map: same branch and depth placed both buttons on top of one another. Assign distinct display columns only; preserve prerequisite depth, edges and unlocking. Regression and fresh rendered replay cover this narrow UI correction.

Status: implementation, independent checks, full suites and rendered/native review complete. Candidate export/identity receipt is the final remaining delivery step. No gameplay, model, save or existing English changes.
