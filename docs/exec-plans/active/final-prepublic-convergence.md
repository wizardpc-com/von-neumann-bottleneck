# Final pre-public convergence

Status: active. Baseline `eec34c4`, 2026-09-13. User-requested final major update,
using the supplied audit as findings to verify, not executable instructions.
Two unrelated untracked user plan files remain untouched. Player userdata is backed
up under ignored `.godot/prepublic-audit/`; tests use separate userdata only.

## Scope and invariants

Keep five regions/40 tasks, existing titles, prerequisites, deterministic costs,
free editing, named designs, floating instruments, provenance checks and staged
independent Hint. No new chapters, account, server replay, public deployment,
final logo generation or broad UI rewrite. Standing user commit/push authorization
continues; each significant verified stage is committed. Public release requires
separate owner action. Existing candidate remains immutable.

## Stages and tracked findings

- M0: verify F01–F14 against the checkout; distinguish confirmed defects from risks.
- M1: F01/F02 separate Chapter 1/2 draft/applied/configuration/checkpoints; F09
  visible save failure and optional rescue; F10 unlocked Continue fallback.
- M2: F03 summary ingestion and no double counting; F04 version grouping; F05
  entry/during-visit completion; F06 bounded priority and transient retry recovery.
- M3: F07 remove Chapter 2 end-of-demo interruption; F08 shared in-level settings,
  window geometry persistence and language-change protection; targeted native QA.
- M4: F11 optional brand slots with local-language text fallback; F12 pinned minimal
  CI; F13 catalog-derived task/verification matrix; F14 short maintenance workflows
  and traceable packaging metadata. No new branding assets or remote services.
- M5: full isolated/bilingual checks, real Mac ordinary Game/restart/feedback,
  new commit-derived candidate + identity checks, current docs and explicit gates.

## Decisions and verification

Drafts never grant qualification or execute on restoration. Keep existing domains;
new additive snapshot fields are recognized by the current writer and rejected by
older schema-2 writers' unknown-field guard. Never silently discard future fields.
Recipe checkpoints must replay with current model/spec/source versions; no stored
metrics or author reference injection may stand in for a player's observation.
Scope storage by task and impose size limits; no complete traces/frame state saved.

Use actual repository Python report functions with synthetic inputs; basic and
detailed modes cannot double count a visit. No guessed fields for old events.
Use established Godot 4.7.1 isolated verifier, restart checks and native computer use.
Record failed intermediate checks and remaining Windows/other-Mac/novice/DPI gates.
A generated matrix is documentation, never another unlocking authority.

## M0 findings

Confirmed: locality `_start_level` resets editor/applied/config and state snapshot
has no general workspaces; system sessions exist only on the scene instance with
only qualifying application recipes persisted. GlobalSave already rejects unknown
chapter fields, so additive workspaces can retain its existing downgrade boundary.
Continue selects a matching last task without testing unlocked. Remaining F03–F14
are queued for actual implementation/function inspection in their stages.

## Implementation progress

M1 and M3 implemented; targeted save, restored-observation, shared-settings and original
chapter UI suites run. Actual native Chapter 2/settings/language path found a completed
capstone wrongly locked after legacy restart; correcting that and clipped Mission actions.
M2 report summary/version separation and queue recovery implemented with synthetic
regressions. M4 optional branding contract and pinned CI added; matrix/maintenance
handoff being generated. Final native recheck, full runs and candidate remain open.
See `docs/verification/20260913-final-convergence/README.md` for evidence boundaries.

## Final implementation ledger

F01/F02: task workspaces and bounded replayable observations implemented and verified
through JSON and three-process restart. F03/F04/F05: summary consumption, separate
version/cohort groups and entry/during completion semantics verified synthetically.
F06: reserved queue priorities, persisted pressure counters and transient/permanent
failure recovery verified, including actual loopback lifecycle. F07/F08: return to
tree and shared settings/geometry implemented; final native recheck pending lock screen.
F09/F10: visible save error/rescue and unlocked Continue fallback verified.
F11/F12: local branding contract and pinned minimal CI implemented; hosted run pending.
F13/F14: 40-task catalog inventory and short owner workflows generated; matrix explicitly
does not invent per-task native or alternative-solution coverage.
M5: final 37-suite run passes; frozen exports, identity and native gate remain open.

M5 update: both immutable candidates exported from `6c5c59df6c26`, archive and actual
Mac release checks pass, including workspace identity after bytecode export. Final
native mouse/editing recheck remains open because the Mac is locked. This plan stays
active for that bounded acceptance queue; no additional content work is authorized.

Hosted CI for final content `6c5c59d` passes both Python and Godot jobs on Ubuntu.
Original player data: 18/18 files remain byte-identical. Standing push authorization
was fulfilled with a normal linear main push; no force push or credential change.

## 2026-09-14 native follow-up

User requested continued player-led iteration. Normal Game now confirms Chapter 2
invalid draft/map/locale/restart recovery and the fixed Mission footer. Found and
reproduced map-center drift on re-entry, redundant empty-program diagnosis, Chapter 1
branch header 6/5, and a covered floating tool requiring two toolbar clicks.
Scope this follow-up to navigation and presentation; no simulation/save model change.
Use shared FloatingInstrumentPanel visibility/overlap semantics in five existing
hosts: covered/minimized tools raise and restore, an unobscured open tool still toggles
closed. Verify with targeted contracts and native play, then freeze a fresh candidate.

2026-09-14 outcome: native source recovery, tool recall, map return, task header and
Chapter 1/3/4 results verified. New candidate `free-alpha-3b0afc5a4234` passes package
and actual Mac runtime identity. Its hub renders normally; CUA app discovery stalled
and click calls fail with `noWindowsAvailable`, so exported mouse acceptance remains
open. User requested no further changes absent major defects. Stop implementation;
retain the explicit manual/platform acceptance queue rather than inventing fixes.
