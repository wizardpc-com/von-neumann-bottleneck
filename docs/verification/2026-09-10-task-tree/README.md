# 2026-09-10 任务树与应用支线验证

**开发完成部分：** 34 节点总任务树、五个可选应用任务、三个附加目标、两个兼容方案延续入口，以及本地试玩记录 v2 / 主动反馈 / 多会话离线报告。原主线、自由电路、独立三级 Hint 和恢复重验保留。**完整原生验收尚未完成，不是发布验收通过。**

## 已验证

- Godot 4.7.1 stable，Mac Apple M2。
- `20260909T175025Z-08d6f144`：资源导入、隔离目录探针及 **24 套测试全部通过**，见 `results.json`；最终会话关闭和反馈焦点调整另做定向检查，见 `final-playtest-*.txt`。
- 新构建任务：空白内部、参考行为/确定性、无连线反例、独立三级提示不污染方案；五条可选支线全部加入恢复重验检查，坏支线不丢原主线。
- 新系统任务：只读一次覆盖不同长度、零、溢出、重复值及等价 DSL，常量假答案失败；两张订单在实际模型的 27 个部件组合上标定。预算 24，计算目标 66 周期有 3 个解，搬运目标 320 周期有 4 个解，没有一个配置同时达标。见 `order-calibration.jsonl`。
- 附加目标：实际执行路线、闲置部件不计路线、两方案恢复、重复搬运/未使用预取被换出、成本 4 的 138 周期路线、复制不覆盖、不直接授予完成。
- 记录：单调前台/后台/反馈分段；完成不结束 visit；失败后的重试与成功后优化分开；中断间隔未知；关闭自动记录仍可填反馈；缺失评分为 null；案例和运行关联；嵌套源码字段过滤。
- `python3 -m unittest discover -s tests -p test_playtest_report.py` 通过。另使用 ClockStore 合成导出完整执行报告 CLI，重复导出去重成功，产生 HTML/JSON；未使用真实玩家反馈作公开测试资料。

## 本轮真实原生操作

通过普通 Game 的总任务树进入「错了一位」，阅读任务后从元件台拖出四个 XOR，手工完成九条连接，调节播放频率，正式测试 **32/32 通过**。返回任务树后显示完成状态，视野保留。没有调用参考解或写入进度标记。

操作中发现直达任务的 Mission 被工具窗覆盖，已修正首次层级；完成卡重复显示同一段文字，已改为不同的收获与下一步文案。两项最终效果仍需下一次原生启动复查。

旧 overlap QA 档中的 CPU 方案无法通过当前恢复重验，导致后续域关闭；实际玩家目录未改动。另一个已实际玩过的 exploration QA 档，原主线全部通过只读重验；已复制为独立 `task-tree-20260910` QA 档，原档保留。

## 未闭合项

- 中文普通 Game 输入回放 `20260909T174209Z-55dbde1c` 319 项中 4 项失败，见 `gui-failure-before-replay-fix.txt`。检查截图发现频率输入后的固定“空白”落点位于新默认打开的元件卡上，误触放置；框选区域也越入元件窗。回放现改为可见左侧空白落点、通过真实 dock 关闭挡住框选的工具窗，**修正后尚未重跑，不能计为通过**。
- 继续启动原生试玩时，computer use 明确返回 Mac 锁屏且无法自动解锁。已请求用户解锁；未绕过锁屏，也未以自动测试替代原生操作。
- 待解锁后：原生完成四路择一、报警、只取一次及两张订单；检查任务树锁定/搜索/平移缩放/返回、Hint 确认取消、工具拖放、Retina 与焦点切换；验证反馈 F8、部分回答、导出及正常退出重启；复跑中文/英文 Game 输入路线和 CPU。
- Windows 导出与真正新玩家的理解、节奏和趣味性尚未验证。

## 复现

按 `docs/development/testing.md` 和 Mac handoff 运行 `scripts/verify-project.py`；新一轮验证必须继续使用独立目录。原生 QA 副本在 `.godot/mac-playtest/task-tree-project`，其专用数据目录为 `VonNeumannBottleneckChecks/task-tree-20260910`；启动携带 `--playtest-source=agent_native`。该路径只用于开发，不属于玩家发布入口。
