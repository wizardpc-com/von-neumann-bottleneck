# Settings completion follow-up

Baseline 18f1b71. Scope remains the approved non-copywriting prelaunch work:
no chapter, simulation, unlock, persistent game format or public endpoint changes.
User downloads and original player data remain untouched.

## Repairs

- Diagnostic exports choose a free filename and check flushed write status; rapid
  repeat exports preserve earlier reports.
- A newly visible export-folder action joins the settings Tab cycle immediately;
  hidden actions leave that cycle. Focus scrolling and fixed footer remain intact.
- Reset and data-deletion confirmations use bilingual button keys. The persistent
  feedback dialog refreshes after language changes; deletion text wraps within bounds.
- Progress-independence assertions exclude only snapshot generation time. An initial
  full run exposed a second-boundary false failure, not changed progression. All
  game data and other snapshot fields still participate in the assertion.
- Distribution guidance now points to the current evidence and schema-2 writer guard.

## Evidence in progress

Chinese ordinary Game input replay: 602 checks, zero failures, isolated run
`20260911T143524Z-a3404cee`. All conventional suites except the timestamp assertion
passed there. Three affected assertions passed in fresh isolated profiles after
repair. Final English regression exposed the new persistent-dialog language assertion: built-in buttons are internal children and the normal translation walk skipped them. Explicit dialog-property refresh fixes that defect; final targeted and frozen-candidate receipts follow below.

Source-native Mac: restored English/mute/33% settings from the prior normal exit;
opened and confirmed reset, observed Chinese/60 FPS/audio on/70%; first-choice prompt
remained dismissed. English and Chinese title/body bounds are clear. Cancel returned
without resetting; diagnostic export plus two Tab presses visibly focused the newly
shown folder button. Inspection caught default English confirmation buttons in Chinese,
now repaired; final native recheck follows below.

Windows, another Mac, outside beginners and long mixed-DPI/focus stress remain
separate owner gates. No public deployment, upload or release.

## Requested functionality coverage

| Request | Implementation / acceptance |
| --- | --- |
| Chinese/English persistence | Hub choice and startup preference; native normal-exit restoration, bilingual settings tests |
| Sound switch and volume | Effects bus, persisted mute/level, safe default restore; native control + bus tests |
| Grouped settings with fixed close | Display/audio/privacy/support scroll, Tab follows focus, fixed footer; 1280×720 contract + native UI |
| Reset defaults | Confirmation restores presentation only; complete game snapshot and consent independence checks |
| Build/support tools | Build ID, data/log folder, local playtest and bounded diagnostics exports; native export and preservation test |
| First configured receiver notice | Explicit local/basic, inventory, no default upload/backfill; prior native local choice + fresh endpoint tests |
| Privacy separation/offline behavior | Existing v2 transport, independent opinions/scores, empty production endpoint; remote/summary/local receiver evidence retained |
| Boundary/UI review | Both Game input routes, persistent feedback translation, native reset/cancel/Tab; Windows/mixed DPI/long sessions still pending |

No Mimo or narrative rewrite was attempted, as requested. Language keys added here
only make functional confirmation controls follow the chosen interface language.

## Final source verification

All conventional suites have passing results after the scoped fixes. Initial results
are kept honestly as initial (one failed assertion in each); final affected-suite logs
are separate, not edited into the old run. Chinese and English ordinary Game replays
each passed 602 checks. Final settings, feedback UI and shared UI checks pass on the
actual final runtime; all checks used dedicated QA directories.

## Final native and frozen candidate receipt

Content `80105a7f5ea6e0c1d0d5017ad81029fbfaa2930d`, frozen build
**free-alpha-80105a7f5ea6**. Mac and Windows archives, packaged notes and hashes agree
([manifest](candidate-manifest.json)). Actual Mac binary probe passes build identity,
40 tasks, Game-only mode, excluded developer files, empty endpoint and upload off
([probe](mac-package-probe.json)). Windows export remains untested on Windows.

Final source-native reset dialog shows Chinese 确认/取消, with readable title/body;
cancel works. Actual mouse enters the task tree and completion board; board displays
0/40, five regional counts, main/side/bonus counts, no individual task list. QA-only
loopback configuration explains its optional community toggle; production remains empty.
Normal source exit succeeded. The earlier locked-screen reset gate is now closed.

The exported QA copy was given a unique bundle identity and re-signed; the delivery
app/archives were untouched. Its correct-build homepage rendered. CUA mouse commands
returned noWindowsAvailable, including after window transition; keyboard navigation
was not reliable enough to count as a completed route. This is **launch/render evidence
only**, not a mouse smoke-test pass. Stopped only this own isolated candidate process.
The owner candidate mouse gate remains open; source interaction is separate evidence.
