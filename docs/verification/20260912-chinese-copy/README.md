# 中文文案复审与导入 — 2026-09-12

本轮采用用户最新的中文风格要求，在 MiMo 与逐条审计结果上继续编辑，
因此没有机械照搬附件要求的固定标题和 30/12/34/6 采用比例。
标题容许隐喻与省略，规格、操作和关键结论保持直接表达。没有为了用典而添典故。

## 来源与范围

- 基线提交：`18b37fcb99731f89d26ca522acc22aaf6676a963`。
- 附件 ZIP SHA-256：`69c0af180a7142a68466f3c0cab2cbca44cc0a597ef3020b24f41f61cc9d4e73`。
- `source-decisions.jsonl` 保留附件全部 82 条原始审计记录；
  `final-decisions.jsonl` 记录每条最终措辞、分类、原因及原仓库措辞。
- 82 个审计基线哈希匹配；77 个真实翻译键相对指定源码基线没有冲突。
  其中 38 条与尚未导入的编辑基线不同，不能误报为仓库更新冲突。
- 本机没有原 MiMo 输入、输出和验证器包。**没有重跑或宣称原验证器 3/3 PASS**。
  使用本目录 `check_import.py` 独立校验来源、保护内容和导入差异。

最终 82 条裁决：人工修订 59，采用 MiMo 8，退回审计基线 10，保留不变 5。
这是相对编辑基线的裁决，不是仓库修改数量。实际改变 108 个中文 PO 键，
另同步第四章页眉、6 个标题与任务树 26 个紧凑标题（同一文案的展示入口，不是新增任务）。
全部差异在 `import-manifest.json` 与 `title-surfaces.json` 中列明。

## 标题与叙述

| 区域 | 标题 | 说明仍承担的任务 |
| --- | --- | --- |
| 序章 | 一线成机 | 逻辑、运算、存储与双分支构建 |
| 第一章 | 时间的去处 | CPU、RAM、Bus 与等待证据 |
| 第二章 | 少走远路 | 缓存、局部性与工作集 |
| 第三章 | 与等待并行 | 缓冲、反压、预取与重叠 |
| 第四章 | 各就其位 | 布局、整理成本与有限空间 |

部分任务采用“留住一瞬、交替接力、先行一步、化整为零”等题目。
半加器、寄存器、RAM 等组件身份继续使用技术名称。
第三章首页增加独立 `overlap.hub.eyebrow` 中文副标题；原分支“汇合挑战”不变。
本轮仅优化中文：所有已有英文值保持不变，新英文键沿用原可见的 `Synthesis`。
英文技术副标题的重新编辑留给后续英文文案工作。

语义复核修正了以下问题：

- 第一章“不要求软件优化”限定到主线，避免与已有减少重复读取支线冲突。
- 综合关“只改一处”限定为第一次对照，不限制后续组合优化。
- 全加器说明明确 A+B 还要加上输入进位。
- 偶校验保留先翻一位、再翻第二位的漏检反例。
- 带宽受限结论指向搬运器，避免推导出增加缓冲对整机永远无效。
- 序章转场兼容两条路线的不同完成顺序；教程总结不再用过时的红绿颜色定义 0/1。

数字、英文技术标识、占位符、术语链接与正式 Hint 均受保护。
模型、用例、任务 ID、门槛、玩家方案和存档规则没有改变。

## 排版修正

实际回放截图发现旧序章地图的“谁能通过？”与“错了一位”共用同一分支、
同一前置深度，按钮及标题完全重叠。现在只分配不同显示列，原始深度与依赖边不变。
`test_hardware_prologue_ui` 增加同深度、不同位置的回归检查。
总任务树的布局算法与玩法均未改动。

## 验证边界

- 中文目录、英文不变、保护标记、导入范围与标题同步：独立脚本 PASS。
- 首轮中文全量隔离检查 PASS；普通 Game 自动回放 602 检查、0 失败。
- 原生 Mac 普通 Game：鼠标打开首页与任务树，查看第二/三章综合任务详情；
  首页五卡、技术标签和说明完整。中文切换英文、正常退出、重启保留英文、再切回中文成功。
- 原生定位以英文稳定任务标识完成；工具的中文输入未可靠送达，**不记作中文 IME 验收通过**。
- 复核实际渲染的教程完成、LOAD/STORE 完成、序章双分支地图与小窗口任务说明。
  自动回放截图与原生观察分别记录，不把两者混为真人通关。
- 排版修正后的最终复跑、候选身份与包路径见本页后续验收记录。
- Windows 实机、导出 Mac 鼠标接管、另一台 Mac 安装、外部新手、长时 DPI/焦点仍待验收。
  不公开发行，不配置公网服务；真实玩家数据未用于本轮检查。

## 最终源码验收

排版修正后全量与中文 Game 回放再次通过（`final-rendered-results.json`，602/0）。
随后补齐两处通关章节名和第四章页眉，再次全量隔离检查 36 项均通过
（导入、存档目录探针及 34 套测试，`final-source-results.json`）。
两个语言的 40 任务详情边界检查包含在其中；现有英文措辞独立比较为 0 改动。
最终原生普通 Game 再次经首页进入旧序章地图，三个控制支线标题分开显示，
返回首页、正常退出成功；中文偏好重启后保留。

中途一次普通沙箱导入因不能保存 Godot 编辑器设置而失败；允许该应用正常保存
设置后重新运行通过。这次环境失败不计入成功结果，也没有改动测试断言绕过。

所附 `rendered-*.png` 来自真实 Godot 渲染的自动回放，不是手工原生截图。
